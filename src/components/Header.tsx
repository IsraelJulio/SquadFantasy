interface HeaderProps { onHome: () => void; compact?: boolean }
export function Header({ onHome, compact = false }: HeaderProps) { return <header className={`site-header ${compact ? 'site-header--compact' : ''}`}><button className="brand" onClick={onHome} aria-label="Ir para o início"><span className="brand__mark" aria-hidden="true">4F</span><span><strong>4Fantasy</strong>Legends</span></button><span className="header-chip">MODO FUTSAL</span></header> }
